# ── Pipeline vidéo ──
moviepy>=2.0.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Collecte news ──
feedparser>=6.0.0
requests>=2.28.0

# ── Audio TTS ──
edge-tts>=6.1.9

# ── Publication YouTube (optionnel) ──
# Installé automatiquement dans le workflow si YOUTUBE_TOKEN_JSON est présent
# google-api-python-client>=2.100.0
# google-auth-oauthlib>=1.1.0

# ── Optionnel : Kokoro TTS (voix locale, ~85MB) ──
# kokoro>=0.9.4
# soundfile>=0.12.0
